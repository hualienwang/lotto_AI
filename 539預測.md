# 今彩539預測模型說明

本專案的預測功能由後端 `app.py` 的 `/api/predict` 提供。前端只傳入預測模式，後端會讀取 `lotto-539.db` 中的 `history` 歷史開獎資料，計算每個號碼的分數，再用加權抽樣產生 5 個不重複號碼。

> 注意：彩票開獎本質上具有隨機性，以下模型只能作為資料分析與參考，不代表能保證命中。

## 共通資料流程

1. 從 `history` 讀取歷史資料，並依期次由新到舊排序。
2. 將資料庫中的號碼字串，例如 `01,06,18,25,36`，轉成 1 到 39 的整數陣列。
3. 統計不同特徵：
   - 全期開出次數
   - 近 30 期開出次數
   - 近 10 期相對前 20 期的升溫程度
   - 遺漏期數，也就是距離上次開出隔了多久
   - 馬可夫轉移機率
   - 共現矩陣搭配強度
4. 將各特徵正規化為 0 到 1，避免不同統計尺度互相壓過。
5. 依照預測模式套用不同權重，得到每個號碼的綜合分數。
6. 使用加權抽樣選出 5 個號碼。分數越高越容易被選中，但不會固定只取前 5 名。
7. 將號碼排序後回傳給前端。

## 加權抽樣

系統沒有直接選分數最高的 5 個號碼，而是使用加權抽樣：

- 每個號碼都有一個模型分數。
- 分數越高，被抽中的機率越高。
- 每次抽中後會從候選池移除，確保 5 個號碼不重複。
- 每個號碼會加上一點基礎權重，避免低分號碼完全沒有機會。

這樣做的好處是結果不會過度僵化，同一種模型多次產生時仍會有變化。

## AI智能預測

用途：綜合型模式，兼顧長期頻率、近期熱度與冷號回補。

權重：

- 全期頻率：40%
- 近 30 期頻率：35%
- 遺漏期數：25%

作法：

1. 統計每個號碼在全部歷史資料中的開出次數。
2. 統計每個號碼在最近 30 期中的開出次數。
3. 計算每個號碼距離上次開出隔了幾期。
4. 將三者正規化後加權合成。

適合用在想要取得較均衡參考號碼的情境。

## 頻率分析

用途：偏重長期高頻號碼。

權重：

- 全期頻率：80%
- 近 30 期頻率：20%

作法：

1. 以所有歷史開獎資料為主，找出長期出現較多的號碼。
2. 加入少量近期頻率，避免只看很久以前的累積資料。

這個模式會傾向選出歷史上較常出現的號碼。

## 冷熱分析

用途：偏向近期較冷、遺漏較久的號碼。

權重：

- 近期低頻程度：45%
- 遺漏期數：40%
- 全期低頻程度：15%

作法：

1. 計算近 30 期頻率，頻率越低分數越高。
2. 計算遺漏期數，越久沒開分數越高。
3. 參考全期低頻程度，讓長期少出現的號碼也有機會。

這個模式不是追熱門，而是偏向尋找近期沉寂的號碼。

## 走勢分析

用途：偏重近期升溫的號碼。

權重：

- 近 30 期頻率：45%
- 近 10 期相對前 20 期升溫程度：45%
- 全期頻率：10%

作法：

1. 計算最近 10 期每個號碼的出現率。
2. 計算再往前 20 期每個號碼的出現率。
3. 用最近 10 期出現率減去前 20 期出現率，得到升溫分數。
4. 混合近 30 期頻率與少量全期頻率。

這個模式會偏向近期突然變活躍的號碼。

## 馬可夫預測

用途：分析跨期轉移關係，也就是某些號碼出現後，下一期哪些號碼較常跟著出現。

權重：

- 馬可夫轉移特徵：70%
- 近 30 期頻率：20%
- 全期頻率：10%

作法：

1. 將歷史資料改成由舊到新排序。
2. 逐期觀察前一期與下一期的號碼關係。
3. 對每個來源號碼建立轉移次數表：
   - 來源號碼是前一期出現的號碼。
   - 目標號碼是下一期出現的號碼。
4. 將轉移次數除以來源號碼的總轉移次數，形成近似轉移機率。
5. 取最新一期的 5 個號碼作為目前狀態。
6. 彙總這 5 個號碼對 01 到 39 的轉移分數。
7. 混合近期頻率與全期頻率，避免只靠轉移矩陣造成資料稀疏。

這個模式適合觀察「上一期開出組合之後，歷史上下一期常見什麼號碼」。

## 共現矩陣預測

用途：分析同期搭配關係，也就是哪些號碼常在同一期一起出現。

權重：

- 共現矩陣特徵：65%
- 近 30 期頻率：25%
- 全期頻率：10%

作法：

1. 對每一期的 5 個號碼建立兩兩搭配統計。
2. 若號碼 A 與號碼 B 在同一期出現，就增加 A 到 B 的共現次數，也增加 B 到 A 的搭配關係。
3. 取最新一期的 5 個號碼作為參考組合。
4. 計算 01 到 39 中每個號碼與這 5 個號碼的共現總分。
5. 混合近期頻率與全期頻率，讓模型同時考慮搭配與熱度。

這個模式適合觀察「哪些號碼常和最近一期的號碼一起出現」。

## 版路分析預測

用途：結合尾數分佈與連號走勢的進階分析。

權重：

- 尾數頻率：40%
- 連號頻率：30%
- 近 30 期頻率：20%
- 全期頻率：10%

作法：

1. 統計 0 到 9 每個尾數的開出頻率，並對應到號碼。
2. 統計歷史開獎中連號（如 12, 13）出現的頻率。
3. 混合熱度指標，挑選具有特定版路特徵的號碼。

## 智慧過濾機制 (Smart Filtering)

為了讓預測結果更符合大數法則，系統在產生號碼後會自動進行過濾，若不符合以下條件則會重新抽取（最多嘗試 50 次）：

1. **總合區間限制**：5 個號碼的總和必須在 70 到 130 之間（這是 539 最常見的總和範圍）。
2. **奇偶平衡**：避免出現全奇數或全偶數的組合。
3. **大小平衡**：避免出現全大（20-39）或全小（01-19）的組合。

這項機制能有效排除極端機率的組合，提供更有參考價值的預測。

## API 使用方式

呼叫格式：

```text
GET /api/predict?type=ai
```

支援的 `type`：

- `ai`：AI智能預測
- `frequency`：頻率分析
- `cold`：冷熱分析
- `trend`：走勢分析
- `markov`：馬可夫預測
- `cooccurrence`：共現矩陣預測
- `balanced`：均衡分佈預測
- `ensemble`：整合策略預測

## 均衡分佈預測

用途：考慮奇偶與大小數值的平衡，避免選號過於集中。

權重：

- 近 30 期頻率：30%
- 奇偶平衡分數：35%
- 大小平衡分數：35%

作法：

1. 統計最近 10 期開獎號碼的奇偶比例。若奇數偏多，則增加偶數號碼的權重。
2. 統計最近 10 期開獎號碼的大（20-39）小（01-19）比例。若大數偏多，則增加小數號碼的權重。
3. 結合近期頻率進行預測。

這個模式適合追求號碼分佈較為平均的玩家。

## 整合策略預測

用途：結合多種核心特徵的綜合策略。

權重：

- 全期頻率：20%
- 近 30 期頻率：20%
- 馬可夫轉移特徵：30%
- 共現矩陣特徵：30%

作法：

1. 同時計算號碼的長期與短期熱度。
2. 引入馬可夫跨期轉移機率與共現搭配強度。
3. 加權合成最終分數。

這個模式是目前系統中考慮特徵最全面的預測方式。

回傳資料包含：

- `period`：預測期次
- `numbers`：預測號碼
- `method`：預測方式名稱
- `description`：模型說明
- `sample_size`：使用的歷史資料期數
- `recent_window`：近期統計視窗
- `top_candidates`：分數最高的前 10 個候選號碼

